
### Переменные
- VITE_API_URL — базовый URL API
- VITE_API_TIMEOUT — таймаут запросов (мс)

### Значения по умолчанию
- .env.development: VITE_API_URL=http://localhost
- .env.production: VITE_API_URL=https://your-domain.com

## Пошаговое выполнение (СИНХРОННЫЙ РЕЖИМ)

### Шаг 1: Создать файлы окружения
- [ ] Создать .env.example с комментариями:
  VITE_API_URL=http://localhost
  VITE_API_TIMEOUT=30000
- [ ] Создать .env.development:
  VITE_API_URL=http://localhost
  VITE_API_TIMEOUT=30000
- [ ] Создать .env.production (пока заглушка):
  VITE_API_URL=https://your-domain.com
  VITE_API_TIMEOUT=30000
- [ ] Добавить .env* в .gitignore (кроме .env.example)

**Критерии приёмки:**
- [ ] Файлы созданы в корне проекта
- [ ] .env.example закоммичен, остальные .env игнорируются

➡️ **После выполнения шага:**
1. Отметить выполненные пункты
2. НЕ КОММИТИТЬ
3. ОСТАНОВИТЬСЯ и показать результат

**После моего подтверждения:**
- ✅ Если всё ок → коммит chore: add environment config files
- 🔄 Если нужны правки → исправить

---

### Шаг 2: Обновить API клиент
- [ ] В api/client.ts использовать переменную окружения:
  const API_URL = import.meta.env.VITE_API_URL || 'http://localhost';
- [ ] Обновить baseURL: `${API_URL}/api/v1`
- [ ] Добавить проверку, что URL доступен (опционально)

**Критерии приёмки:**
- [ ] При npm run dev клиент обращается к http://localhost/api/v1
- [ ] При npm run build (после сборки) используется значение из .env.production

➡️ **После выполнения шага:**
1. Отметить выполненные пункты
2. НЕ КОММИТИТЬ
3. ОСТАНОВИТЬСЯ и показать результат

**После моего подтверждения:**
- ✅ Если всё ок → коммит feat: use environment variable for API URL
- 🔄 Если нужны правки → исправить

---

### Шаг 3: Обновить типы и документацию
- [ ] Добавить в types/index.ts типы для env (опционально)
- [ ] Обновить README с инструкцией по настройке окружения

**Критерии приёмки:**
- [ ] Документация обновлена

➡️ **После выполнения шага:**
1. Отметить выполненные пункты
2. НЕ КОММИТИТЬ
3. ОСТАНОВИТЬСЯ и показать результат

**После моего подтверждения:**
- ✅ Если всё ок → коммит docs: add environment setup instructions
- 🔄 Если нужны правки → исправить

---

## Финальный чеклист
- [ ] .env.development и .env.production созданы
- [ ] API клиент использует переменную окружения
- [ ] В dev-режиме фронт обращается к localhost
- [ ] В prod-режиме используется правильный URL

## Статистика выполнения (заполняется агентом после каждого шага)
После завершения каждого шага добавь в этот раздел:

### Шаг N
- Создано/изменено файлов: X
- Добавлено строк кода: Y
- Выполнено пунктов: Z/Z
- Потрачено токенов (оценка): ~N